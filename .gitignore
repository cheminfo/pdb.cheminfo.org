.claude
.DS_Store
.env

# State the server's deploy script keeps inside the checkout it deploys.
.deploy
docs/
compose.yaml.local.bak
coverage/
# Both of these are generated: the top-level rsynced PDB mirror, and the
# backend's runtime SQLite files. Anchored on purpose — an unanchored `data/`
# also matches src/**/data/, which holds real source.
/data/
backend/data/
backend/public/
frontend/playwright-report/
test-results/
node_modules/
